api:
  insecure: true
  dashboard: true

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    network: "ianoie-proxy"
    exposedByDefault: false
    watch: true

entryPoints:
  web:
    address: ":80"
